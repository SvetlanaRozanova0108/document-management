<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="003" author="Svetlana Rozanova">

        <!-- Создание таблицы approval_register -->
        <createTable tableName="approval_register">
            <column name="id" type="bigint" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="document_id" type="bigint">
                <constraints nullable="false" foreignKeyName="fk_register_document" references="documents(id)"/>
            </column>
            <column name="approved_by" type="varchar(100)">
                <constraints nullable="false"/>
            </column>
            <column name="approved_at" type="timestamp">
                <constraints nullable="false"/>
            </column>
            <column name="registration_number" type="varchar(50)">
                <constraints unique="true" nullable="false"/>
            </column>
        </createTable>

    </changeSet>
</databaseChangeLog>